/*
These 2 variables need to be passed to the script:

job_name = ${env.JOB_NAME}
release_version = ${env.release_version}
*/

current_project = Hudson.instance.getItems().find{ it.name == job_name }
release_project = Hudson.instance.copy(current_project, job_name + "-" + release_version)

